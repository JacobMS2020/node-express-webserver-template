version: '3.9'

services:
  node:
    container_name: node-testing
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./node:/usr/src/app
    networks:
      - webnet

networks:
  webnet:
    external: true
